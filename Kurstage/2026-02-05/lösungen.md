# Lösungen

## Aufgabe 1

```
INPUT operand1
INPUT operand2
INPUT operator

IF operator is "+" THEN
    OUTPUT operand1 + operand2
END IF
IF operator is "-" THEN
    OUTPUT operand1 - operand2
END IF
IF operator is "*" THEN
    OUTPUT operand1 * operand2
END IF
IF operator is "/" THEN
    OUTPUT operand1 / operand2
END IF

```