Wir haben kennengelernt:

* `git checkout BRANCHNAME` um zu einem Branch zu wechseln
* `git branch BRANCHNAME` um einen neuen Branch anzulegen
* `git branch -d BRANCHNAME` um einen Branch zu löschen
* alle drei Commands haben entsprechungen in der VS Code UI bzw. der VS Code Command Palette (Strg+Umschalt+P)